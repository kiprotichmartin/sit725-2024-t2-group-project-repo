//require("dotenv").config();
//const express = require("express");

//const PORT = process.env.PORT;

//const app = express();

const CONFIG = {
  //API_BASE_URL: `http://localhost:${PORT}`, // Ensure this matches your backend server
  API_BASE_URL: `http://localhost:3000`, // Ensure this matches your backend server
};

//app.listen(PORT, () =>
//  console.log(`Frontend Server started on port: http://localhost:${PORT}`)
//);